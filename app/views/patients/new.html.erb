<div class="container my-4 d-flex justify-content-center">
  <div class="w-50">
    <h3 class="text-center mb-4">
      <%= @patient.new_record? ? 'New Patient' : @patient.full_name %>
    </h3>

    <%= render 'form', patient: @patient %>
  </div>
</div>
